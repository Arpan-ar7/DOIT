// Live backend, deployed on Render by your friend.
// Works the same from simulator, emulator, and physical device — no more
// localhost/LAN IP juggling, since this is a real public URL.
export const API_BASE_URL = 'https://doit-8jx9.onrender.com/api/v1';